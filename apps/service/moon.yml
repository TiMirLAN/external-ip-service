$schema: "https://moonrepo.dev/schemas/project.json"

language: python
layer: application

tasks:
  run.dev:
    command: uv run external-ip-service

  lint:
    command: uv run ruff check --fix

  format:
    command: uv run pre-commit run -c ./.pre-commit-config.yaml
